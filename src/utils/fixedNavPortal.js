/** Mount node in `RootLayout` — sibling to `SpatialAnimatedOutlet`, above route content (Framer transform breaks `position: fixed` inside the outlet). */
export const FIXED_NAV_PORTAL_ID = 'erik-portfolio-fixed-nav-portal';
